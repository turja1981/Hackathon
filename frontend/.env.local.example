# Backend API URL (Next.js rewrites proxy this to avoid CORS in production)
NEXT_PUBLIC_API_URL=http://localhost:8000

# App metadata
NEXT_PUBLIC_APP_NAME="LifeSci AI"
NEXT_PUBLIC_APP_VERSION="1.0.0"
